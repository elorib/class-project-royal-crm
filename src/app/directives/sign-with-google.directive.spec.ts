import { SignWithGoogleDirective } from './sign-with-google.directive';

describe('SignWithGoogleDirective', () => {
  it('should create an instance', () => {
    const directive = new SignWithGoogleDirective();
    expect(directive).toBeTruthy();
  });
});
