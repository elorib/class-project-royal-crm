import { CustomersFilterPipe } from './customers-filter.pipe';

describe('CustomersFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomersFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
